import { ButtonAuthorityDirective } from './button-authority.directive';

describe('ButtonAuthorityDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonAuthorityDirective();
    expect(directive).toBeTruthy();
  });
});
